# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
*.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env
.env
.env.local
.env.*.local

# Source spreadsheets (raw xlsx with customer financial data — never commit)
_source_xlsx

# ETL cache (regeneratable — re-download via _etl_scripts/sync_hubspot.mjs etc.)
# Includes raw HubSpot API exports with stripe IDs and owner emails. Always
# stays local; ETL pulls fresh copies on demand.
_etl_scripts/cache

# Note: public/snapshots/*.json IS committed so Vercel deploys have the data.
# The repo MUST remain private. Anyone with repo access can read this data.

# Editor
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.swp
*.swo
.DS_Store
